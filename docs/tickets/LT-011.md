# LT-011 — Connect GitHub and enable static Pages delivery

[Live kanban card](https://app.notion.com/p/3d907031acb581068951e97254ba3ac6?pvs=204) · Wave 1 · C — Discover and learn

## Objective
Give each reviewed increment a simple hosted demo with no backend operations.
## Scope
Use the existing public repository karuhanga/latent-threads. After LT-003 produces the app build, run checks/build locally and publish only the contents of dist plus .nojekyll to the root of a gh-pages branch. Configure Pages branch publishing at /latent-threads/. Keep source/docs on main. Browser access is authenticated and Git pushes work; use those available tools or complete CLI authentication only if needed. A separate repository and custom Actions build workflow are unnecessary for this first route.
## Acceptance criteria
- [ ] GitHub owner/name/visibility are recorded as karuhanga/latent-threads, public. Verify an available write/deploy route without putting tokens in files or chat.
- [ ] Only reviewed changes are committed/pushed. Preserve Lincoln’s initial tracked ideation archive; keep raw docs/private artifacts out of the deployed dist output.
- [ ] Pages eligibility is checked for the selected account/visibility; private-source Pages is not misrepresented as private site access.
- [ ] A repeatable local publish command uses the pinned lockfile, runs checks/build and publishes only dist contents plus .nojekyll. Record the source commit in each deployment commit; preserve branch history and make reviewed releases reproducible.
- [ ] Open the real Pages URL, refresh a deep link and verify assets/data resolve under its repository subpath. Record URL and commit; rollback is redeploying a known-good revision.
## Dependencies
[LT-003](LT-003.md)
Lincoln created public `karuhanga/latent-threads`; browser and system Git access are verified. The provider billing issue was resolved. The repeatable local publisher now works with system Git ahead of bundled fallback tools in PATH. Full Wave 1 is hosted: source `8ded5ff`, deployment `3cb2bcf`, [Pages success](https://github.com/karuhanga/latent-threads/actions/runs/34673341278). Live URL, refreshed focal context and exact release provenance were verified; [detailed evidence](../pm/wave-1-review.md). No user account action remains.

## Verification
Inspect repository visibility, local check/build evidence, deployment-branch contents and source commit, Pages deployment result, real hosted home and refreshed focal-node URL after publication.
## Out of scope
No paid plan purchase, custom domain, backend, secret-bearing deployment or uploading raw conversations.
## Handoff
Ticket status and acceptance evidence are tracked in Notion; these unchecked boxes define the contract, not a live progress mirror.
Planned role: Platform. Size: S — up to half day. Estimates indicate focused engineering effort, not a calendar commitment.
When starting, set an actual Agent, move to In progress and name files owned. For review, attach exact changed files/commit, relevant check results and a visible preview when applicable. PM checks acceptance before Done.
## References
Product scope: [First playable release](../product/first-release.md)
Delivery plan: [Small increments](../pm/delivery-plan.md)
Local source: `docs/ideation/latent-threads-export/02-product-research-brief.md`

## Latest verified release
Wave 2 six-domain release is deployed: source `6a5f51170b7272ee5e972ccf94b46c768ca6b6d3`, deployment `7ee42b226495da19338e7fe951e482d3573f8861`. Exact-source CI, Pages, public release metadata, refreshed software route and coffee example were verified. [Wave 2 release evidence](../pm/wave-2-review.md). Access and deployment mechanism are unchanged; later documentation-only commits preserve this application release.
