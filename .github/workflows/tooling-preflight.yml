name: Tooling preflight

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runner-check:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Confirm hosted runner access
        run: |
          echo "GitHub-hosted runner is available."
          node --version
          git --version
